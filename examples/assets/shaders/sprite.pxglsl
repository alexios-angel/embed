#version 330 core

#include "sprite.uniforms.pxglsl"

in vec4 position;
in vec2 tex;
in vec4 color;
out vec4 pixel_color;

void main(  ) {
	pixel_color = texture( Texture0, tex ) * color;
}
